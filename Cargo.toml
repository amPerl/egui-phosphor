[package]
name = "egui-phosphor"
description = "Phosphor icons for egui"
version = "0.13.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/amPerl/egui-phosphor"

[dependencies]
egui = { version = "0.35", default-features = false }

[features]
default = ["regular"]
thin = []
light = []
regular = []
bold = []
fill = []
# Compile-time font subsetting via the `subset!` macro. Adds no dependencies.
# No cost to include all variants, since a subset only embeds the icons you name.
subset = ["thin", "light", "regular", "bold", "fill"]

[dev-dependencies]
eframe = "0.35"

[[example]]
name = "multiple_variants"
required-features = ["thin", "light", "regular", "bold", "fill"]

[[example]]
name = "subset"
required-features = ["subset"]

[[test]]
name = "subset"
required-features = ["subset"]

[[test]]
name = "multi_variant"
required-features = ["subset"]
